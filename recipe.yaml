package:
  name: sqlite
  version: "0.1.1"

source:
  path: .

build:
  script:
    - mkdir -p $PREFIX/lib/mojo
    - cp -r sqlite $PREFIX/lib/mojo/

requirements:
  host:
    - mojo ==0.26.3.0.dev2026042005
  run:
    - mojo ==0.26.3.0.dev2026042005

about:
  summary: SQLite bindings with morph ORM integration for Mojo
  license: MIT
